from random import random
for i in range(10000):
  regions = 0
  if radint (0,100) <= 87:
    regions = regions +1
    if radint (0,100) <=65:
      regions = regions +1
      if radint (0,100) <=17:
        regions = regions +1
